# ISSUES — bugs, limitations, and open risks

> **What belongs here:** defects, known limitations, and risks that cut **across** phases — anything
> that could bite during operation or testing. One row per issue, newest first within each table.
>
> **What does NOT belong here:** build progress. `PROGRESS.md` is the phase board and the append-only
> handoff log; it stays the single source of truth for *what has been built*. This file is the single
> source of truth for *what is wrong or unfinished*. When they overlap, link — never restate.
>
> **How to use it:** every issue gets an id (`WC-nn`, never reused). When you fix one, move its row to
> **§2 Resolved** with the commit sha; do not delete it. The **"Affects testing?"** column exists so
> the owner can tell at a glance whether an open issue should change how they run a session.
>
> **Status:** `OPEN` · `INVESTIGATING` · `PLANNED` (a plan doc exists) · `OWNER-ACTION` (not a code
> fix) · `ACCEPTED` (known trade-off, no fix intended) · `RESOLVED`.
>
> Last updated: 07 Aug 2026 (WC-29 / WC-R19, P18; **WC-04 re-researched and raised to High**).
> **WC-25, WC-26 and WC-28 are resolved in code; WC-25's runner half needs the PWA deployed before it
> does anything.** WC-04 now has a full write-up at `docs/plan/wc-04-speaker-attribution.md` —
> research only, one decision open, nothing built.

---

## 1. Open

| id | Status | Sev | Affects testing? | Issue |
|---|---|---|---|---|
| **WC-01** | OWNER-ACTION | **Low** (was High) | **No** (was Yes) | **The live Apps Script Web App is the P13 build; only P15a's `Code.gs` delta is undeployed.** ⚠ **The original diagnosis here was wrong** and is kept visible rather than edited away: it said the deployment was P11 and that pushes write no `role_title` and no per-folder `README.md`. Verified against the live folder 04 Aug 2026 — `profile.json` **does** carry `role_title` with P15a's `" / "` join, and `README.md` **is** written; its `\| **Role** \|` header (singular) is what dates it to P13, since P15a emits `\| **Role(s)** \|`. So the entire missing delta is two things: storing the unjoined `role_titles` array, and that label. **Neither is read by the brain** — P16a recovers both hats by splitting the joined string (`lifecycle.profile_role_titles`), which is exactly why P15a kept `role_title` writable. Still worth the 2 minutes (Apps Script editor → *Manage deployments* → edit → **New version**, same `/exec` URL) and **required before P16b**, which wants the array. ⚠ `apps-script/` remains the one component `git push` does NOT deploy. |
| **WC-02** | OWNER-ACTION | High | **No longer** | **Two participant folders existed for the same human:** `kishor-goswami-business-analysis-specia-8750` and `…-f25b` — the P13 re-onboarding signature (`phase-13` §14). **Owner moved `…-f25b` out of `participants/` on 05 Aug 2026** and `cli list-participants` now returns one active participant instantly. The feared consequences never materialised: the graph held **zero activities** at the time (only the 10 seeded roles), so nothing was ever corroborated or conflicted on one human's say-so. `…-f25b` was never readable (see WC-07) so its contents are still unconfirmed — it is **moved, not deleted**, and can be inspected once Drive hydrates it. **Recurrence is the live risk:** with several testers this multiplies by headcount. After everyone's first session, check `cli list-participants` against the actual headcount. |
| **WC-03** | **RESOLVED** | High | — | **A role a multi-hat person *declared* had no owner, so handoffs to it looped forever.** Fixed in P16a — see §2 Resolved, `WC-R7`. |
| **WC-04** | PLANNED · **researched 07 Aug 2026** → `docs/plan/wc-04-speaker-attribution.md` | ~~Medium~~ **High** | **Yes** (was "mildly") | **The extractor doesn't know who is speaking, so it can't tell which hat did the work.** `Extractor.extract(answer)` gets one answer's text and nothing else; `role_titles` never reaches the brain's extraction path. So *"I write the BRD"* only gets a `PERFORMS` edge if the **answer itself** names a role. During testing this shows up as activities with no owning role, thin per-role SOP sections, and roles appearing on `cli coverage`'s invite list even though the person *is* that role. ~~**Does not affect interview quality.**~~ ⚠ **That last claim was wrong and is kept visible rather than edited away** (06 Aug 2026, phase 17): blind extraction is the direct cause of **WC-24** (one answer forking into several activities plus an invented `role.deployment-approver`) and a contributing cause of **WC-25** (a denial carries no positive assertion, so it is silently dropped and the wrong `PERFORMS` edge survives). Both were felt by testers, and both ended sessions early. ~~Severity is unchanged only because P17b/P17c address the symptoms more cheaply than threading identity through extraction would.~~ ⚠ **Severity raised to High on 07 Aug 2026, and the "activities with no owning role" symptom above is measurably NOT the problem** — a probe over the live graph found **20 of 21 activities already carry a performer** (the one exception is a junk node). Attribution is *present* and mostly *right*, but only because both testers are single-hat and the extractor is guessing from content stereotype ("BRD" ⇒ BA) — right by coincidence, silently wrong the moment a hat doesn't match its cliché. **What actually raises the severity is P17b:** the adjudicator now weighs `[performed by …; in stage …]` heavily (*"a different performing role -> almost certainly 'new'"*), so imprecise attribution now **causes node forks**. `act.discovery-work` and `act.detailed-project-discovery` are one activity in two nodes, same stage, differing only in performer set (`{BA,SA}` vs `{BA,DS,SA}`) — i.e. **WC-04 now feeds WC-24**, which is the issue testers actually felt. Full write-up, four-layer approach, risks and an A/B rebuild measurement plan: `docs/plan/wc-04-speaker-attribution.md`. **One decision blocks building it** (ambiguous dual-hat work → both hats or neither); §8 there. → P16b. |
| **WC-05** | PLANNED | Medium | No | **A dual-hat person's altitude is undefined** when their two hats sit at different `REPORTS_TO` depths. `alignment._persona_role` picks one role (plan-15 risk **R5**), which decides whether a divergence is a reported finding or a defect to reconcile. Needs an org chart *and* a divergence to matter, so it can't bite single-person testing. → P16c. |
| **WC-06** | PLANNED | Low | No | **SOP presentation for multi-hat people** — cosmetic; work done under two hats should appear under each, with a note so the reader isn't surprised to meet the same name twice. → P16d. |
| **WC-07** | ACCEPTED | Medium | **Yes** | **Reads against the Drive bus can hang for minutes** — `G:` is Google Drive for Desktop over Dokan; when its request pool is exhausted every operation blocks or fails with `WinError 1450`. `fsretry.py` covers the *raising* case, not the *blocking* one; only "Available offline" fixes hangs (done for the bus folder, per P14). **Hit twice in this session**, including on `cli docgen`, which reads the bus for persona display names (`persona_display_names(FolderBus(...))`). **Workaround:** `cli docgen --bus <local scratch path>` generates the deliverable without touching Drive (names render as raw persona ids). |
| **WC-08** | **RESOLVED** | Low | — | **`cli list-participants` produced exit 0 and no output.** Not a regression — it was WC-07 blocking on one specific folder. See §2 Resolved, `WC-R8`. |
| **WC-09** | ACCEPTED | Medium | Rarely | **Nothing re-examines two accounts that already merged as "same".** `MISALIGNMENT` needs a node flagged `CONFLICTING`, which comes from the adjudicator at ingest (`resolve.adjudicate` — a real semantic judgement, so genuine divergence *is* catchable). But if two accounts merge as *same*, no later pass revisits them. This is the remainder of ADR #23: P15c added the **retention** (`Provenance.account`) that a batch semantic tier would need, so the tier is now *possible* — just not built. |
| **WC-10** | ACCEPTED | Low | No | **Readable participant ids are guessable.** P13 traded an unguessable UUID for `name-role-4hex`, so any app user could push into another's folder. Fine for trusted colleagues; needs a per-participant token before untrusted users. (`phase-13` §14.) |
| **WC-11** | OPEN | Low | No | **PWA icons are placeholders** — the manifest references `icon-192/512.png`, generated by a zero-dep script rather than designed. |
| **WC-12** | OPEN | Low | No | **STT field accuracy has never been measured.** ElevenLabs Scribe is verified working on *clean* audio only. Before trusting the permanent-truth transcript, run `node scripts/stt-eval.mjs <dir>` from `pwa/` over ~20 real messy recordings (Indian-accented English, noise, SKUs/jargon) and record the WER here. |
| **WC-13** | **CONFIRMED** → see **WC-24** | **High** | **Yes** | ⚠ **This is no longer a risk — it happened** (06 Aug 2026). Three people produced **52 activity nodes**, with one real activity forked four ways; the predicted failure mode is exactly what occurred. The prediction below stands as written, and the concrete instances plus the fix plan are tracked in **WC-24**. Original text: **Node dedup has never run at multi-person scale, and P16a-bis's arithmetic depends on it.** "Three BAs each describe a quarter, so a quarter is missing" only works if all three quarters land on the **same** nodes. If *"user story document"*, *"US doc"* and *"the story doc"* fork into three nodes, each carries its own three-quarters-empty gap set and nothing ever closes — the feature would look broken when the real fault is resolution. The machinery is present and healthy (alias table + `fastembed 0.8.0` **verified installed** 04 Aug 2026 + the LLM adjudicator), it has simply never been exercised by more than one voice. **First multi-person round: check the activity node count in `_graph/index.md` against what you expect before trusting anything else.** (phase-16 R7.) |
| **WC-14** | **RESOLVED** | Medium | — | **The closing screen's *Done* button was live while the Answer Log was still being pushed to Drive.** Found by the owner in live testing, 05 Aug 2026. Fixed in the same session — see §2 Resolved, `WC-R9`. |
| **WC-15** | **RESOLVED** | Medium | — | **A test user said "I tested it" but never tapped *End & save*, so nothing was pushed.** Reported by the owner 05 Aug 2026. Fixed in the same session — see §2 Resolved, `WC-R10`. |
| **WC-16** | **RESOLVED** | High | — | **WC-R10's own fix didn't fire in the owner's post-deploy retest — the nudge/banner never appeared.** Found 05 Aug 2026, same day as WC-R10 shipped. Fixed in the same session — see §2 Resolved, `WC-R11`. |
| **WC-17** | **RESOLVED** | Low | — | **While the bot "thinks", the previous question stays fully readable, so a person can't tell if it's still waiting on an answer.** Reported by the owner 05 Aug 2026 during the same retest. Fixed in the same session — see §2 Resolved, `WC-R12`. |
| **WC-18** | **RESOLVED** | Low | — | **The save nudge (WC-R11) used the same green as the rest of the UI, so it didn't stand out.** Reported by the owner 05 Aug 2026. Fixed in the same session — see §2 Resolved, `WC-R13`. |
| **WC-19** | **RESOLVED** | Medium | — | **WC-R12's blur-and-dots fix for the "thinking" state took over more of the screen than intended** — reported by the owner as feeling like a different UI, not the minimal fix asked for. Fixed in the same session by replacing it outright — see §2 Resolved, `WC-R14`. |
| **WC-20** | **RESOLVED** | **High** | — | **The Session Brief told the interviewer each person held five or six roles they never declared.** Found by the owner from two live sessions, 06 Aug 2026. Fixed the same session — see §2 Resolved, `WC-R15`. |
| **WC-21** | **RESOLVED** | **High** | — | **"You wear both hats" was asserted to people who had merely *described* another role's work.** Both testers denied it; one was asked a third time after two denials. Fixed the same session — see §2 Resolved, `WC-R16`. |
| **WC-22** | **RESOLVED** | **High** | — | **A brief was one question repeated twelve times** — 11 of 12 threads asked "who picks it up next?". Fixed the same session — see §2 Resolved, `WC-R17`. |
| **WC-23** | **RESOLVED** | Medium | — | **Role-inherited threads named a role the listener does not hold** ("Another *Account Management Specialist* described…", said to a Solution Architect). Fixed the same session — see §2 Resolved, `WC-R18`. |
| **WC-27** | **RESOLVED (code)** · **OPEN (data)** | **High** | **Yes** | **P17b's anti-splitting rule suppressed other node types: Objectives 4 → 0, ApprovalPoints 1 → 0.** Found by checking the rebuild's full node-type table rather than just the activity count it was meant to improve. The lost content was real — *"Team Work Quality Verified Before Project Completion"*, *"Understand Complete Process"*. Cause: the new rule sits immediately after *"Prefer FEWER, well-formed nodes"* — two emphatic minimise-instructions back to back — and the model generalised it from "don't split one activity into three" to "emit less". **This is worse than the duplication it was fixing:** ADR #32 makes Objectives the finding the engagement sells, and the extractor prompt is the one place that can silently delete them. Fixed by scoping the rule (*"THIS RULE IS ABOUT NEAR-DUPLICATE ACTIVITIES AND ROLES, NOTHING ELSE"*) and pinned by `test_anti_splitting_rule_is_scoped_and_cannot_suppress_other_node_types`. ⚠ **The data is still missing** — the four Objectives return only on the next `rebuild-graph`, since their answers are already marked ingested. *Lesson: a prompt rule is not a local edit; the acceptance check has to count what you were NOT trying to change.* → phase-17 §5.4. |
| **WC-24** | **PARTLY RESOLVED** (06 Aug 2026) | **High** → Medium | **Yes, less so** | ✅ **Rebuild landed: Activity 62 → 49, Role 14 → 11.** The invented `role.deployment-approver` and `role.development-lead` are gone, and **every activity now carries a performing role and a stage** (the probe finds zero unplaced pairs), which is the input ADR #40 needs to keep improving. The ceiling decision is validated on fresh data: at 0.90 nothing merges wrongly, at the old 0.86 four pairs would, at the originally-planned 0.78 thirty-eight would. **Still open:** cross-answer forks the extractor cannot see — `create-user-story-documents` vs `user-story-creation`, `submit-user-story-document` vs `submit-final-user-story-document`, `development-oversight` vs `monitor-progress`. The four code-review nodes became three, not one. The remaining lever is retrieval quality (candidate recall), not the prompt. Original report: **One real activity has forked into four graph nodes, so it gets asked about four times.** `act.code-review` / `act.code-review-and-team-oversight` / `act.code-and-function-quality-review` / `act.review-checks` are one thing; so are four discovery nodes and four documentation nodes. **52 activity nodes from three people.** This is **WC-13 confirmed** — no longer "never exercised at scale"; it has now happened, and it is what produced the tester's *"Did I not tell you that I document the user stories?"*. Two sub-causes: one answer splits into several activities plus an invented role (`act.review-checks` + `role.deployment-approver` share a timestamp with `act.code-and-function-quality-review`), and `similarity_ceiling = 0.86` plus an adjudicator that never sees the performing role fails to re-merge them. **Cleanup is a rebuild, not surgery** — the Answer Logs are the immutable source of truth and all six exist (five live + one in `_archive`). → P17b, `docs/plan/phase-17-interview-fidelity.md` §5. |
| **WC-25** | **RESOLVED** (06 Aug 2026) | **High** | **Yes** | **A denial is now recorded, and it has a scope.** Was: *"SA does not do anything in quality assurance"*, *"I do not demo a solution to the client"*, *"the project timeline is not my job"* (three times) — none of it written anywhere, so the wrong edge survived and the question returned next round. Fixed in P17c (ADR #43) across both planes: `answer-log.schema.json` carries an optional `classification`; the runner writes the value it was already computing and closes the refused thread in-session whatever the model reported; `lifecycle.refusals` re-derives per-persona refusals from the logs each round; the Planner drops the matching threads (and, for `not_mine`, the matching `known_facts`). The extractor gained the negation rule, so a denial no longer mints the PERFORMS edge it denies. **Two scopes, not one as planned:** `dont_know` closes the question, `not_mine` closes the whole piece of work — with P17a clustering three threads per node, thread-scope alone re-asks the other two next round. ⚠ **Needs the PWA deployed:** the brain half is inert until a phone writes `classification`, and the phone must be fully reopened (cached service worker). *Lesson: the signal existed for two phases and was discarded one line before it could be persisted — check what the hot path already computes before adding a way to compute it.* |
| **WC-26** | **RESOLVED** (06 Aug 2026) | Medium | **Yes, mildly** | **The brief now tells the interviewer what it already knows.** Was: `briefDigest()` carried a persona summary and the open threads and no "already established" block, so the model could not acknowledge prior knowledge — a tester said *"I had replied in my previous sessions"* and *"Did I not tell you…?"* in one session. Fixed in P17c (ADR #44): `session-brief.schema.json` gains `known_facts`, the Planner writes 8–12 deterministic one-liners from the persona's own subgraph (*'You do "Compile Final BRD" (in Pre-sales Phase) — produces Final BRD; hands to Delivery Specialist'*), and the runner renders them **above** the brief with a hard rule against asking for any of it cold. Never model-written: a hallucinated memory is worse than none. **Composes with WC-25** — an activity someone refused as `not_mine` is removed from `known_facts` too, which an end-to-end run against the live graph caught and no unit test would have: the fix for this issue was re-asserting the thing the other issue exists to stop. |
| **WC-28** | **RESOLVED** (06 Aug 2026) | **High** | **Yes** | **Every handoff-confirm question went to *both* people — describing a colleague's work made you a holder of their role.** Reported by the owner off the first post-P17b briefs: the Delivery Specialist's *"It sounds like Business Analysis Specialist hands 'Compile Final BRD' over to you — do you receive it?"* was **priority 1 on the Business Analyst's screen too** — the handoff Kishor had just described **making**, put to him as its recipient. Not one duplicate: **all four** `handoff_confirm` threads were in both briefs, and two of them named roles nobody in the engagement holds (Account Management Specialist, Solution Architect). Cause: `crosspersona._role_owner_personas` counted "has provenance on an activity this role PERFORMS" as owning the role. Kishor said *"the Delivery Specialist creates the pre-sales timeline"*, which stamped his provenance on `act.create-pre-sales-timeline`, and that made him a Delivery Specialist for routing. This is the **WC-R5 over-reach** that ADR #37 removed from `_persona_summary` and ADR #38 from the dual-hat copy — routing was the last place still trusting contribution as identity, because P16a *added* declaration (ADR #34) to the P9 inference instead of replacing it. It stayed invisible while one person was on the bus. Fixed by ADR #42: declaration is the only source of role ownership; a role nobody declared is `route_discoverer`, which is what that verdict has always meant. **Note the narrow fix is not enough** — "prefer declared owners, else fall back per-role" still sends an ownerless role's confirms to whoever described its work, which is half the bug. Pinned by four tests in `test_role_scoping.py` (all four fail on the old code); the fifth fences the legacy branch. Both live briefs regenerated. *Lesson: a heuristic that is merely imprecise with one voice in the graph becomes wrong with two — re-audit the single-source proxies before every headcount increase.* |
| **WC-29** | **RESOLVED** | Medium | — | **The opening question of every warm session was a template with a database label wedged into it — and it was the one turn the model never saw.** Reported by the owner from live testing, 06 Aug 2026. Fixed the same session — see §2 Resolved, `WC-R19`. |

---

## 2. Resolved

| id | Sev | Fixed in | Issue |
|---|---|---|---|
| **WC-R19** | Medium | *(P18, uncommitted)* | **The first question of a warm session was printed, not written** (was WC-29). The owner was opened with *"Welcome back Kishor. It sounds like Solution Architect hands 'Provide Technical Solutions and Effort Estimates' over to you…"*. Two faults, and the second is the one that mattered. **(1) The quoted name is a graph index label.** `Activity.canonical_name` is specced in the extractor prompt as *"a short normalized name (2-4 words)… the node's identifier"* — built to be **matched**, not spoken. `planner._opener_and_followups` interpolates it into `f"It sounds like {giver} hands '{name}' over to you…"` and `runner.start()` printed the result verbatim, so a keyword landed mid-sentence. **(2) Turn one could not see the memory.** `start()` never called the model, so `WHO YOU'RE TALKING TO`, `WHAT WE ALREADY KNOW` (WC-26, built one day earlier for exactly this) and the covered list were all invisible on the one turn that sets the tone — the opening question was *structurally* incapable of acknowledging what the person had already told us, whatever the brief contained. Fixed by routing the warm opener through the same `decide()` call every other turn uses (ADR #45): `start()` is now `async`, the brief's opener is handed over labelled *"machine-written scaffolding, NOT a script"* with a rule never to read the quoted label aloud, and a **no-yes/no rule** because the graph is built from `raw_answer` alone — a tidier opener that invites *"yes"* is a worse opener. **The thread is pinned before the call and the model's `active_thread_id` is ignored on this turn**, so filing is unchanged and turn one is the only turn whose attribution is not the model's call. Cold start stays deterministic (nothing to reword, and it keeps a first-ever session instant). **A template fallback is the safety**: `start()` could not fail before this, so the LLM error path falls back to the old greeting + template rather than opening on a dead screen. Cost is one model call before the first question, shown with the existing "thinking" state (*"Getting your session ready…"* — the usual copy would have been a lie, since nothing has been said yet). *Lesson: "the model may reword this" was true of every turn except the first, and the first is the one that decides whether the person believes the app remembers them.* |
| **WC-R18** | Medium | *(P17a, uncommitted)* | **A role-inherited thread named a role the listener does not hold** (was WC-23). A Solution Architect's last question before he ended his session was *"Another **Account Management Specialist** described 'Send Proposal', but we never captured who picks it up afterwards."* `_role_threads` had scoped correctly — the thread was inherited because `role.solution-architect` performs that node — but the copy read `t.role_name`, which is `Gap._attributed_role`: the **first** role performing the node. On a node two roles perform, that is routinely the other one, so the sentence named a role with nothing to do with why the question was being asked. `_role_threads` now records which declared role dragged each node in and stamps it onto the thread, so the opener and `_ROLE_WHY` both become coherent at once. *Lesson: two different questions — "whose gap is this?" and "why are YOU being asked?" — had been answered by the same field. Reporting attribution and routing rationale are not the same fact and cannot share a slot.* |
| **WC-R17** | High | *(P17a, uncommitted)* | **A brief was one question repeated twelve times** (was WC-22). 11 of 12 threads in the Solution Architect's brief asked *"who picks it up next?"* — five consecutively, about five different activities — and `reserve_threads` was visibly banded (4× `next_handoff`, then 11× `trigger`, then 9× `output`). Cause: `_FIELD_IMPACT` ranks by **field**, so `threads[: self._max]` took one field across every activity — a column-major read of a table whose rows are the person's work. It also made the interviewer unable to obey its own `SYSTEM_PROMPT`, which says *"finish a stage before moving to the next one"* — no prefix of that ranking can support it. New `_cluster()` groups a brief by node (`_CLUSTER_MAX = 3` threads per node), applied to own/role-inherited/orphan threads alike, so a brief walks 3–4 activities at 2–3 fields each. `reserve_threads` became a set difference rather than a slice, with its own test, because the carried set is no longer a prefix. The tester answered turn six with *"I mean, it's a continuous process"* — the question not fitting — and was asked it three more times before leaving. *Lesson: ranking individual items correctly does not produce a coherent agenda. The unit the person experiences is the topic, not the item, so selection has to group by topic even when scoring cannot.* |
| **WC-R16** | High | *(P17a, uncommitted)* | **"You wear both hats" was asserted from mere mention** (was WC-21). `dual_hat = persona in giver_personas` read `_role_owner_personas`, which is *declarers* **∪** *contributors of the role's activities* — so describing what QA does made the graph conclude you were the QA Head. A Solution Architect who declared one role was opened with *"when you switch from your Quality Assurance Head hat to your Technical Specialist hat…"*; a Business Analyst got the Delivery Specialist version a third time after denying it twice — *"I told you I do not act as a delivery specialist. Why you're not trying to understand?"* Dual-hat copy now requires a **declaration on both sides** (`_declared_owners`, ADR #38). **Routing is untouched** — the same people still receive the thread; only the wording narrows, and a non-declarer gets the standard confirm copy, which is answerable. *Lesson: the union in `_role_owner_personas` was right for its original purpose (WC-R7: never fail to ask the real holder) and wrong for a purpose it was later reused for. "Whom should I ask?" tolerates a guess; "what are you?" does not. Auditing every caller of a widened definition is part of widening it.* |
| **WC-R15** | High | *(P17a, uncommitted)* | **The brief claimed each person held five or six roles they never declared** (was WC-20). `persona_summary` leads `briefDigest()` and is injected at the top of the live prompt on **every turn**, and it read roles off the persona's own subgraph — every Role node they have provenance on. Mentioning a role is how provenance lands there, so a Solution Architect who declared one role was briefed as *"Business Analysis Specialist, Development Lead, Quality Assurance Head, Solution Architect, Technical Specialist"*, and a Business Analyst as six roles including **`Customer`**. The interviewer duly asked both about other people's jobs, and both ended their session early. Now declared roles only (ADR #37); with nothing declared the sentence drops its `As …,` prefix rather than guessing. **This is WC-R5's over-reach in the most damaging position available to it** — and `_persona_role_ids`, in the very next method, already refused to make the same inference for exactly this reason. *Lesson: when a fix establishes a rule ("contribution is a guess, declaration is a fact"), grep for every other place that makes the same inference — WC-R5 fixed `alignment.py`, P16a-bis fixed `_persona_role_ids`, and this third site sat between them, untouched, for two phases.* |
| **WC-R14** | Medium | `a9be5b6` | **The "thinking" fix (WC-R12) took over more of the screen than intended** (was WC-19). The blur-and-bouncing-dots overlay was meant to sit only on the transcript card, but the owner saw it read as a different UI taking over the screen, not a minimal state change. Replaced outright rather than tuned: the transcript card now simply doesn't render at all while `status === "thinking"` (`{currentLine && status !== "thinking" && (...)}` in `SessionScreen.tsx`) — no overlay, no blur, no new element. The existing `.wc-stage-hint` line ("Thinking about what you said…") and the bot's own thinking animation are left to carry the signal, exactly as they did before this whole thread of fixes started. All the blur/overlay CSS (`.wc-transcript-wrap`, `.wc-transcript-thinking`, `.wc-thinking-overlay`, `.wc-thinking-dot`, `@keyframes wc-thinking-bounce`) was removed rather than left dead. *Lesson: "make the stale state visually distinct" has a cheapest-possible version (hide it) and a fancier version (overlay something on it) — reach for the cheap one first, especially when the fancier one's actual footprint on screen is hard to fully predict without seeing it live.* |
| **WC-R13** | Low | `a9be5b6` | **The save nudge (WC-R11) didn't stand out — same green as the rest of the UI** (was WC-18). `.wc-nudge` and `.wc-nudge-btn` now use an amber/yellow background and border (`#fff8e1` / `#ffca28`) with red text (`#a11`, matching the app's existing error-message red rather than inventing a new one), and the button's pulsing glow (`wc-pulse-amber`) uses the same amber instead of the brand green — a color that's used nowhere else in the UI, which is the point: this is a warning, not a normal accent. |
| **WC-R12** | Low | `3a82c68` | **The previous question stayed crisp on screen while the bot "thought", reading like it was still the live question** (was WC-17). The stage's `.wc-stage-hint` line already said *"Thinking about what you said…"*, but that's a small caption above a full, fully-legible question card — easy to miss, especially mid-conversation. The card now blurs (`filter: blur(4px)`, `opacity: 0.5`) the instant `status === "thinking"`, with three bouncing dots (`.wc-thinking-overlay`) fading in on top of it, so the stale text visibly stops being "the thing to answer" instead of just being narrated as such above it. *Lesson: a status caption next to content and a status change ON the content read very differently to someone mid-task — the caption is easy to skim past; the content itself changing state is not.* |
| **WC-R11** | High | `3a82c68` | **WC-R10's close-detection never fired in practice** (was WC-16), because the model's own `"close"` action is structurally unreachable outside one narrow path: `Runner.respond()` only calls `advance()` (which can emit `"close"`) from inside the `action === "probe"` branch, once that thread's probe budget is exhausted — so a thread that completes via `"opener"`/`"acknowledge"` (the common case: the model says `thread_complete: true` without ever probing) falls through untouched, and nothing re-checks whether that was the *last* thread. On a cold-start session (empty brain, `open_threads: []`) there is no thread list to exhaust at all — the model free-narrates indefinitely and is never told `closing: true`, so per its own prompt rule (*"use only when told the session is ending"*) it structurally never says `close`. The owner's post-deploy retest was very likely a cold-start session, which is exactly the case WC-R10's fix could never reach. Fixed at the runner level, not just the UI: `respond()` now checks directly — once every brief thread is covered on a **non**-cold-start session, it forces `effectiveAction = "close"` and swaps in `THREADS_DONE_UTTERANCE` regardless of what the model chose that turn (guarded by a one-shot `announcedDone` flag so it doesn't repeat on every later turn). Cold-start sessions still can't be force-closed this way (there's nothing to exhaust), so the UI now also carries a permanent, always-visible caption under the *End & save* button (`.wc-savehint`) that doesn't depend on any close-detection at all — the one guarantee that survives every session shape. *Lesson: a "the model signaled it's done" guard is only as strong as the paths that can actually set that signal — trace EVERY branch that could complete the last unit of work, not just the one the original bug happened to go through, and always keep one detection-independent fallback for the case the signal can't exist yet (cold start).* |
| **WC-R10** | Medium | `2f2dc12` | **A tester never tapped *End & save*, so their answers were never pushed — but nothing told them to** (was WC-15). Reaching the end of the interview was purely conversational: `THREADS_DONE_UTTERANCE` (previously `CLOSING_UTTERANCE`, reused for both the organic wrap-up and the post-tap sign-off) said only *"I'll take some time to make sense of this... have a good one"* — nothing named the button, and the composer with the still-unclicked *End & save* button stayed on screen looking like any other turn. Split into two distinct utterances: the organic wrap-up now explicitly says *'tap "End & save" below now'*, and only the post-tap sign-off keeps the plain goodbye. `SYSTEM_PROMPT`'s own `close` action definition got the same instruction, in case the model ever emits it directly. On the UI side, the moment the runner signals `close` a green nudge banner appears above the composer and the button itself gets a pulsing highlight (reusing the existing `wc-pulse` keyframe), and a `beforeunload` handler now blocks silently closing the tab/refreshing while there are captured answers that haven't reached `pushed`/`exists` — a native "leave site?" confirmation is the last line of defence once someone tries to walk away with a session already open. *Lesson: a conversational sign-off is not a save confirmation — if the action that actually persists data is a separate manual step, the thing that tells the user to take it has to survive them not reading carefully, not just exist somewhere in the copy.* |
| **WC-R9** | Medium | `b431737` | **The closing screen let you leave while the Answer Log was still in flight** (was WC-14). `End & save` sets `syncState = "pushing"` and fires `pushAnswerLog`, but *Done* (and the header *Exit*) called `onExit` unconditionally — so a tap during the Apps Script round-trip unmounted the screen mid-push. The person never learned whether their answers landed, and on a failure they were never offered the **download fallback**, which is the session's only other route into the brain. Both exits now wait on the push. That gate needed a second fix to be safe: nothing in the chain (browser fetch → Pages Function → Apps Script) had a deadline, so a silent upstream would have left the promise pending and the person stranded on a dead screen — `pushAnswerLog` is now bounded by `PUSH_TIMEOUT_MS` (45s) and a timeout surfaces as `failed`, which is exactly the state that offers the download. `.wc-iconbtn` also had no `:disabled` styling, so a blocked *Exit* would have looked tappable. *Lesson: disabling the only way out of a screen is only safe once the thing you're waiting on is guaranteed to finish.* |
| **WC-R8** | Low | `2a68dd4` (diagnosis) | **`cli list-participants` returned exit 0 with no output** (was WC-08). **Not a regression.** Diagnosed 05 Aug 2026: the command was blocked inside `bus.read_profile` on the `…-f25b` folder, which `fsretry` was retrying in a loop — `[fs-retry] WARNING: read …-f25b/profile.json: Insufficient system resources exist to complete the requested service`. Proven by running the same command against a **local scratch bus**, where it printed both fixtures correctly. Since `bus.list_participants()` enumerates and every caller then reads *every* profile, one unreadable folder blocks `list-participants`, `docgen`, `coverage`, `run-round` and `retire-participant` alike. Cleared by moving the folder out of `participants/` (WC-02). *Lesson: an operation that hangs is not the same as one that fails, and "exit 0, no output" is a symptom of the caller being killed, not of the code taking an empty branch.* |
| **WC-R7** | High | `2a68dd4` | **A declared role had no owner, so handoffs to it looped on the discoverer forever** (was WC-03). `_role_owner_personas` defined ownership purely as "contributed this role's activities", so a person who genuinely *is* the Technical Specialist — but whose dev work never earned a `PERFORMS` edge under that hat, because the extractor cannot see who is speaking — owned nothing. A colleague's *"I hand the build to the Technical Specialist"* found no owner, returned `route_discoverer`, and asked the **colleague** *"who would know?"* every round while the real person was never asked at all. Fixed by consulting the onboarding declaration (ADR #34): contributors **∪** declarers. *Lesson: this put a deterministic fact — they told us who they are — behind a probabilistic one, whether extraction happened to attribute the work. When both are available, consult the deterministic one first.* |
| **WC-R6** | High | `9583841` | **`cli coverage` crashed on a `cp1252` console.** The matrix used a tick mark (`U+2713`), which does not exist in the codepage Python gets for stdout on Windows — `print()` raised `UnicodeEncodeError` and took the command down. **It passed the live smoke test only because the empty-graph message happens to be ASCII**, so the failure needed real data. Render is now pure ASCII (`[x]` / `[ ]`); the test does `text.encode("cp1252")` — the operation that was actually failing, not a proxy. *Lesson: anything this codebase `print()`s must be ASCII; graph files are UTF-8 and may contain em dashes, terminal output may not.* |
| **WC-R5** | High | `5a6b806` | **Role attribution silently deleted misalignment findings.** `alignment._persona_role` answered "which role IS this person?" by first match over roles performing any activity they'd contributed provenance to. An exec who merely *comments on* someone else's activity picks up provenance on it — so the CEO was identified **as** the Account Management Specialist, both contributors collapsed onto one altitude, and the finding vanished **with no error**. Attribution now scores roles: sole-contributor activities first, then shared, then id. |
| **WC-R4** | Medium | `5a6b806` | **The process diagram and the walkthrough disagreed.** The diagram was grouped into Mermaid subgraphs per stage, but the narrative was still ordered by `activity_flow`, which can only order what artifact/handoff links happen to connect — so it printed Discovery's work *before* Pre-Sales'. Half-done spine rendering is worse than none, because the picture looks authoritative. Stage order now wins; flow order decides within a stage; unstaged work last. |
| **WC-R3** | Medium | `d3351be` | **A planner opener asked the wrong person about the wrong role.** `reports_to` read *"Who do you report to?"* — but a Role gap fires for **any** role in the persona's subgraph, including one they merely *mentioned*, so a BA would be asked about their own reporting line while the thread pointed at the QA Head's node. Both `reports_to` and `performs` now name the role. A test asserts every opener names its node. |
| **WC-R2** | Medium | `cf983db` | **A test passed for the wrong reason.** The "without aliases, resolution forks" test was propped up by a stub LLM that agreed with whatever retrieval ranked first — but with one Role node in the graph the lexical index returns it as a neighbour *whatever* its similarity, so the outcome actually depended on model taste. Rewritten to assert the load-bearing, determinable fact: without the alias table **no exact match protects the decision**. |
| **WC-R1** | High | `cf983db` | **`PROGRESS.md` said P15 had "no code yet" while 25 files of P15a sat uncommitted on disk.** An interrupted session had written most of P15a and logged nothing. *Lesson: on picking up mid-phase, `git status` before trusting the board.* |

Earlier resolved issues (P0–P14) are recorded in the `PROGRESS.md` handoff log rather than restated
here. The notable ones: the P14 Drive `WinError 1450` failures (`fsretry.py`), tolerant reads
swallowing a busy-drive error and causing paid re-ingest, brief distribution recreating archived
participant folders, nodes going permanently silent when their only contributor retired,
`reset-engagement` deleting the real graph when pointed at a scratch bus, and the batch extractor
dying on an unparseable LLM 200.
