# Warp Compass — Decision Log (ADRs)

Running log of architecture decisions. Append a new row when you make one (a tradeoff, a version
pin, a deviation from a phase brief). Keep the *why*. Status: ✅ in force · 🔁 revisit · ⛔ reversed.

| # | Date | Decision | Why | Status |
|---|------|----------|-----|--------|
| 1 | (design) | **Two-speed architecture** — fast live plane + slow batch brain, joined by a 2-artifact contract. | Talking must feel instant; understanding an org is slow. Decoupling lets each be optimized; the contract keeps them independent. | ✅ |
| 2 | (design) | **Discovery-first, no questionnaire**; ontology is a *compass*, not a script. | A fixed question set bakes in BA bias and misses reality. Unbounded on content, structured on completeness. | ✅ |
| 3 | (design) | **Personas discovered, not declared**; prototype = 1 persona per user; role-clustering deferred. | Same anti-assumption principle; never block onboarding. | ✅ |
| 4 | (design) | **Graph-first memory; LLM proposes, deterministic rules dispose.** Raw Answer Log is immutable truth; graph re-derivable. | Relationships are the value; the create gate is the anti-hallucination spine; re-derivability de-risks the DB choice. | ✅ |
| 5 | (design) | **Neo4j Community** for the graph (behind `GraphStore`). | Free, mature, Cypher fits completeness/conflict queries, great viz. Kùzu archived (Oct 2025) so not the foundation; forks are a later swap. | ✅ |
| 6 | (design) | **ElevenLabs** for both STT + TTS, one key (behind `STTProvider`/`TTSProvider`). | Best transcription accuracy (the load-bearing leg), natural voice, many languages incl. Indian. Browser TTS dev-only. | ✅ |
| 7 | (design) | **DeepSeek** LLM — `v4-flash` live, `v4-pro` batch; OpenAI-compatible base URL; retry+backoff. | Cheap fast live calls + capable batch reasoning. Legacy `deepseek-chat`/`reasoner` deprecated (~Jul 2026). | 🔁 confirm IDs at P2/P5 |
| 8 | (design) | **PWA (one codebase) on Cloudflare Pages + Worker key proxy**; shared cloud folder as sync bus. | One installable app for Android+iOS behind a link; HTTPS for mic/install; keys never in the browser; free; manual stand-in for networked v1. | ✅ |
| 9 | 2026-06-28 | **PWA stack = React + Vite + TypeScript.** | Mature PWA tooling (vite-plugin-pwa), typed contracts shared with the worker, best ecosystem for the theme/UI. | ✅ |
| 10 | 2026-06-28 | **Brain stack = Python + uv.** | Matches the design; best Neo4j/LLM/data ecosystem; uv = fast reproducible envs. | ✅ |
| 11 | 2026-06-28 | **Embeddings run locally** — switched `sentence-transformers` → **`fastembed`** (`BAAI/bge-small-en-v1.5`), behind `VectorIndex`; lazy + optional (extra `vectors`), with a zero-dep `HashingEmbedder` fallback. | Keeps cost model intact (no 3rd paid API). fastembed is ONNX-based (no torch) → lighter, matching the owner's lightweight preference. Fallback keeps tests/CI green without the model. | ✅ |
| 12 | 2026-06-28 | **Vector index = portable sqlite brute-force cosine** (numpy dot over L2-normalized vectors), behind `VectorIndex` — replaced sqlite-vec to avoid a native extension on Windows. | Zero native deps, instant at prototype scale (hundreds of nodes); swap for sqlite-vec/ANN later behind the same seam. | ✅ |
| 15 | 2026-06-28 | **DeepSeek model IDs are config-driven** (`DEEPSEEK_MODEL_BATCH`/`LIVE`), defaulting to `v4-pro`/`v4-flash`; `cli check-models` reveals what a key can access. | Config-driven means a wrong default is a one-line `.env` fix, not a code change. **Batch `deepseek-v4-pro` confirmed working** via live ingest (2026-06-28). `deepseek-v4-flash` (live) still to confirm at P5; classic fallbacks `deepseek-reasoner`/`deepseek-chat`. | ✅ batch confirmed |
| 13 | 2026-06-28 | **Neo4j via Neo4j Desktop** (local app, no Docker; Docker Compose kept as an optional alt); Python 3.12+; Node 20+; **npm per JS package** (no workspace). | Owner has no Docker and wants it light. Neo4j runs **only on the laptop, one-time**; end users only open the PWA (never install anything). Keeps Cypher + the browsable graph viz the design values. Data persists across restarts; graph is re-derivable from the Answer Log either way. | ✅ |
| 14 | 2026-06-28 | **Node storage model in Neo4j:** generic `:Node` label (id-unique constraint) + ontology type as a 2nd label; `key_attributes`/`provenance` JSON-serialized. | Cheap type filtering + idiomatic traversals; Neo4j can't store lists-of-maps natively. | ✅ |
| 16 | 2026-06-28 | **Phase-3 completeness modelling choices.** (a) `GraphStore` grew two bulk reads — `nodes_by_type` + `edges` — and the engine scores over an in-memory snapshot rather than per-node Cypher. (b) An Activity's `next_handoff` field is **satisfied by a HANDS_OFF_TO edge OR by producing a final output** (an artifact no activity consumes) — i.e. a legitimate process endpoint isn't pestered for a handoff. (c) "Handoff verified from both sides" is approximated **structurally**: the receiving role must perform ≥1 activity, else it's a one-sided/dangling handoff. (d) Satisfaction thresholds are config-driven (`persona/org_satisfied_threshold`, default 0.9). | (a) One snapshot keeps scoring pure/deterministic/testable without a DB and is reusable by the Planner + doc-gen. (b) Every real process ends somewhere; requiring a handoff on the terminal step would make "satisfied" unreachable. (c) Persona→role isn't modelled yet; the structural proxy tightens for free as the receiving persona is interviewed (graph is re-derivable). (d) A wrong bar is a one-line `.env` change, not a code edit. | ✅ |

| 17 | 2026-06-28 | **Phase-4 Planner choices.** (a) **A persona is identified by provenance `said_by`**; its brief is scoped to its subgraph (nodes it contributed to) — no explicit Persona node/edge yet. (b) Brief `priority` is an **integer rank** (1 = first), derived by sorting the P3 float impact+recency score. (c) **Openers/followups are deterministic templates** (per gap kind/field), with an LLM enrichment hook left for later — keeps briefs free + testable. (d) Free-narration "newly-surfaced threads" are **not** parsed from Answer Logs; they're already gaps on freshly-created nodes and float up via recency. (e) Cross-persona corroboration is a **basic seam**: a one-sided handoff appears in the discoverer's brief naming the receiving role; routing to the receiver is **P9**. (f) Added `jsonschema` (dev-only) to validate briefs against the contract. | (a) Persona↔role is 1:1 in the prototype and re-derivable; an explicit mapping is additive (P9), so provenance is the cheapest correct key now. (b) The schema demands an int ≥1; rank is the natural reading and keeps the float scoring internal. (c) No network in the batch planner keeps tests deterministic; the runner (P5) is where live LLM calls belong. (d) Avoids duplicating truth that's already in the graph. (e) Keeps P4 focused; the seam exists without pre-building P9. (f) Real draft-07 validation of the brain↔runner contract; tiny pure-Python dep. | ✅ |

| 18 | 2026-06-28 | **Phase-5 live-runner choices.** (a) **The `LLMProvider` seam has two real implementations:** `WorkerLLMProvider` (the shipped PWA path — POSTs to the Worker `/llm`, no key in the browser) and `DirectDeepSeekProvider` (Node `fetch` straight to DeepSeek, **verification harness only**), plus a scripted `FakeLLMProvider` for tests. (b) **The turn loop is LLM-driven but the one-probe rule is enforced in code** (`runner.ts`): the model classifies + proposes the next action/utterance, but a thread is probed at most once, then the runner deterministically covers it and advances. Cold-start openers and thread advancement are also deterministic. (c) **Contract types are hand-written TS** mirroring `contracts/*.schema.json` (not codegen); the Answer Log is validated at runtime against the schema via **ajv** in a **Node-only** `validate.ts` (kept out of the browser bundle). (d) **`COLD_START_OPENERS` is duplicated** in `pwa/.../prompts.ts` (mirror of `planner.py`) as a cross-language constant. (e) Added a thin **brain `cli ingest-log <path>`** — the file-level consumer of the answer-log contract — to close the P2–P5 loop now; the **automated** sync-bus polling is still **P8**. (f) Test/run tooling added to `pwa`: **vitest** (node env), **tsx** (the console harness), **ajv**/**ajv-formats**. | (a) The keys-in-Worker rule (ADR #8) governs the *shipped* app, but P5 must still exercise `v4-flash` end-to-end on the laptop — a Node-only direct provider does that without ever shipping a key; same seam, swap the impl in P6. (b) "LLM proposes, deterministic rules dispose" (AGENTS.md) — guarantees the testable "exactly one probe" behaviour and a clean cold start without depending on model whim. (c) The contracts are small and stable; hand-written types are simplest and ajv gives real draft-07 validation, mirroring the brain's `jsonschema` check on its side. (d) Both planes need the same generic openers and there's no shared TS/Py module; a tiny duplicated constant is cheaper than a build step. (e) The runner's "done" requires proving a log is ingestible; `ingest-log` is the minimal honest primitive and leaves P8 to build the cycle/registry on top. (f) vitest matches the Vite stack; tsx runs the TS harness with no build. | ✅ |

| 21 | 2026-06-29 | **Phase-8 sync-cycle choices.** (a) **The bus folder IS the registry** — a `FolderBus` over `participants/{id}/{profile.json, answer_logs/, briefs/}`; a participant exists the moment its folder does, so adding users needs no config. (b) **Persona 1:1 with participant** (`persona_id = participant_id`), and **no `:Persona` node** is introduced — a persona is registered in the graph implicitly via provenance `said_by` on ingest (consistent with ADR #17). The "registry" profile is the participant↔persona mapping in `profile.json`. (c) **Resumability via `profile.ingested_logs`** persisted after *each* log (not just at round end), so a transient DeepSeek failure mid-round never re-ingests a done log; re-ingest is idempotent anyway (graph merges by id). (d) **`RoundRunner` is dependency-injected** (ingestor + planner protocols) so the cycle logic is unit-tested with fakes (no Neo4j/network); the CLI wires the real ones via `_build_ingestor` + `Planner`. (e) **The PWA can't write the shared folder**, so export = download the Answer Log (operator drops it into `answer_logs/`) and import = pick the latest brief file — the manual stand-in the design already chose (DECISION #8); a networked endpoint swaps in behind the same two ops. (f) **Stable participant id in `localStorage`** (replacing P6's per-session random UUID) so the bus folder is stable across sessions. (g) The round does **register → ingest → plan → distribute**; the standalone **cross-persona conflict pass is deferred to P9** (completeness is already folded into planning). | (a) Matches "any number of users, anytime, no config" (§3.4) with the cheapest possible registry. (b) Persona↔role is 1:1 in the prototype and re-derivable; an explicit node would duplicate truth and break the ontology-constraint rule. (c) The expensive, failure-prone step is the LLM ingest; tracking done logs makes a round safely re-runnable and cheap. (d) Keeps the orchestration deterministic and testable, mirroring how P2–P5 isolated logic from the DB. (e) A browser can't touch arbitrary FS; the manual hop is honest for v1 and the seam keeps the upgrade additive. (f) The folder key must outlive a session or briefs can't be routed back. (g) Keeps P8 focused on the transport+cycle; conflicts are their own phase. | ✅ |

| 20 | 2026-06-29 | **Phase-7 voice choices.** (a) **STT uses the batch Scribe model (`scribe_v2`), not the realtime/streaming one** — accuracy over latency (docs/02 §4): the transcript is the permanent Answer Log truth. The browser POSTs the **raw audio blob** as the request body and the Worker wraps it in the multipart form ElevenLabs expects (so the only browser→Worker header is `Content-Type`, keeping CORS trivial). (b) **TTS defaults to `eleven_flash_v2_5`** (fast, widely available) with model + **voice id** as Worker `[vars]` (`ELEVENLABS_TTS_MODEL`/`ELEVENLABS_VOICE_ID`) — one consistent voice for every user regardless of device; switch to `eleven_v3` for more expressive output without a code change. (c) **`BrowserTTSProvider` (Web Speech API) is dev-only**, picked via `import.meta.env.DEV`, so iterating doesn't burn ElevenLabs credits; the shipped path is `WorkerTTSProvider` + `playAudioBlob`. (d) **`audio_ptr` stays `null`** — there is no audio-blob store yet; the verbatim transcript is the truth and a recording pointer waits for the P8 sync bus/storage. (e) **Mic is tap-to-start / tap-to-stop** (`MediaRecorder`, browser-chosen mime webm/opus or mp4); the **typed fallback is always present** (mic denied/noisy/unsupported → `isMicAvailable()` hides the mic, typing still works). (f) **The STT eval gate (≈20 real messy recordings → WER) remains an owner action** — shipped as a zero-dep harness `pwa/scripts/stt-eval.mjs`; P7 is code-complete + locally guard/seam-verified but **not DONE until the gate passes**. | (a) A misheard word corrupts the graph forever; batch Scribe is the accurate leg and turn-based capture means streaming buys nothing. Raw-body + server-side multipart avoids a non-safelisted multipart preflight from the browser. (b) Centralizing voice/model server-side keeps the client contract `{text}` and makes voice tuning a var flip. (c) Honors AGENTS.md "browser TTS dev-only" and protects the cost model while iterating. (d) Re-derivability rests on the transcript; storing blobs is additive and belongs with the bus. (e) "Typed fallback always available" is in the brief; tap/tap is the simplest reliable turn capture on mobile. (f) Vendor benchmarks use clean audio; field audio (Indian-accented, noisy, SKUs/jargon) must be measured before trusting the permanent-truth leg. | 🔁 gate pending |

| 19 | 2026-06-28 | **Phase-6 + context-economy choices.** (a) **The Worker injects the model id** (`DEEPSEEK_MODEL_LIVE` var, `deepseek-v4-flash`) and the key; the browser sends only `messages`/`temperature`/`response_format`. (b) The PWA calls the proxy via **relative paths** (`/llm`, `/stt`, `/tts`) — Vite dev-proxies them to `:8787`, and in prod the Pages-hosted Worker shares the origin, so the same code works untouched. (c) **PWA icons generated by a zero-dep `node:zlib` PNG encoder** (`pwa/scripts/gen-icons.mjs`) instead of adding an image-tooling dependency. (d) On session close the runner **downloads the Answer Log** as the manual hand-off to the brain (the automated sync bus is P8). (e) **Deploy stays an owner action** (CF account) — P6 is "code-complete + locally live-verified", not "deployed". (f) **Process:** every phase brief now opens with a verbatim-signature **Context Card** and the finishing agent writes the next one (AGENTS.md "Context economy"). | (a) Centralizes model choice server-side and shrinks the client contract; swapping the live model is a Worker var, not a PWA rebuild. (b) One code path for dev + prod; no env-specific base URLs to misconfigure or leak. (c) Keeps the toolchain light (owner preference) and reproducible; the mark is regenerable. (d) Proves the artifact end-to-end now without pre-building P8's bus. (e) Can't authenticate to the owner's Cloudflare; everything that *can* be verified locally was. (f) P5 pickup burned ~120k tokens re-deriving code shapes from source; moving the shapes into the brief targets ~30–50k without docs drifting (pointers + signatures, not re-narrated code). | ✅ |

| 22 | 2026-06-29 | **Phase-9 cross-persona choices.** (a) **`crosspersona.py` owns all handoff + conflict threads**; the Planner drops `ONE_SIDED_HANDOFF`/`UNRESOLVED_CONFLICT` gaps from its own gap→thread pass and pulls the routed versions instead (no double-asking). (b) **Bidirectional handoff verification = artifact linkage**, upgrading P3's structural proxy (ADR #16c): a handoff `A→roleB` is **confirmed** when an activity roleB performs CONSUMES an artifact the giving activity PRODUCES; **route_receiver** (thread → the receiving persona) when roleB is an active persona but hasn't linked this flow; **route_discoverer** (thread stays with the giver, the P4 fallback) when nobody's been interviewed as roleB. (c) **A persona *owns* a role iff it contributed that role's activities** (provenance `said_by` on the performed activities), NOT merely by appearing on the Role node — mentioning a role ≠ being it; this is what makes receiver-routing correct. (d) **Confidence promotion via existing node-level + edge re-persist seams only** — `corroborate()` flips `unverified→confirmed` on ≥2-persona non-conflicting nodes (`upsert_node`) and on both-sided handoff **edges** (re-`add_edge`, idempotent MERGE overwrites provenance); **no new `set_edge_status` seam** was added. Made `FakeGraphStore.add_edge` idempotent (merge on the (type,from,to) triple) to match Neo4j. (e) **Cross threads lead the brief** (conflict 1.5 > confirm 1.4 > gap impacts ≥1.0 ≥ trace 0.7). (f) Planner stays **read-only**; the write pass is `cli corroborate --apply` (not auto-wired into P8's `RoundRunner`). | (a) One source of truth for the "connected brain" logic; the gaps still exist for `cli completeness`/scoring, but routing/priority is centralized. (b) "Verified from both sides" must mean *this* handoff, not "roleB exists somewhere"; artifact linkage is the deterministic, testable signal and tightens for free as more is ingested. (c) Provenance on a Role node can't tell the owner from a mention; the activities a role performs can. (d) The Context Card listed only node-level writes; an edge-status setter would force a Neo4j+contract change with no reader (NodeCard/Edge have no top-level `status` field — status lives on provenance), so re-persisting flipped provenance via the existing idempotent `add_edge` is the faithful, lower-surface path. (e) A contradiction or an unverified handoff is more urgent than a missing detail. (f) Keeps the planner pure/deterministic; wiring corroborate into the daily cycle is an additive P8 touch, deferred to avoid silent scope creep. | ✅ |

| 23 | 2026-06-29 | **Phase-9 conflict detection scope (deferred semantic tier).** P9 delivers conflict **routing** (any node flagged `CONFLICTING` by the P2 create-gate, incl. cross-session, → a reconciliation thread routed to **every** contributing persona) + promotion, but **NOT** new *semantic* batch detection of contradictory accounts of an already-merged node. | The current data model keeps **one** canonical card per node (merge absorbs aliases + appends provenance; it does **not** retain each persona's competing description/`key_attributes`), so there is nothing to compare two accounts *from* without a data-model extension (per-persona claim retention) — out of P9's clean scope. Exact-match contradictions are already caught at ingest by the gate (`_flag_conflict`); P9 makes sure they're surfaced to everyone involved rather than averaged away. Semantic batch conflict detection is logged as a future item (see Blockers). | 🔁 revisit |

| 24 | 2026-06-29 | **Phase-10 doc-generator choices.** (a) **One flow, no drift** — extracted `activity_flow(ids, snap)` to `completeness.py`; both the P3 chain check and the P10 end-to-end traversal build on it, and the doc generator consumes `CompletenessEngine.assess()` for the authoritative gap/unbroken verdict instead of re-deriving. (b) **Confidence is read off provenance** (no `NodeCard.status`): effective status = conflicting > confirmed > unverified > proposed; **default renders `confirmed` + always `conflicting`** (a known conflict is never hidden), `--include-unverified` adds the rest with a visible marker. **Gaps are scoped to shown nodes** so a hidden node's gap can't leak its name into the confirmed view. (c) **`traverse`/`render` split** — pure render-agnostic models → Markdown + Mermaid; section labels resolved in `traverse` so `render` is ontology-free; Word/PDF is additive behind the same models. (d) **Section numbering from the taxonomy** — added `Ontology.category_label()` + `categories_sorted()`; the zero-padded codes sort lexically (`05`<`05.1`<`06`<`10`), which *is* the §11 section order. (e) **Self-loops dropped in the diagram/narrative** (presentation only — a generic role handing off to itself), without changing `activity_flow`'s shared semantics. | (a) The end-to-end chain and the completeness chain MUST agree; sharing the builder makes that structural, not hoped-for. (b) Honors "render confirmed by default, surface confidence" while never silently dropping a conflict (the P9 ethos); provenance is the only real status store (the Neo4j `n.status` is write-only). (c) Keeps traversal unit-testable with `FakeGraphStore` (no Cypher) and renderers swappable. (d) Codes already encode the hierarchy; deriving numbering from them needs no extra config and stays consistent with the graph. (e) A self-continuation is visual noise; fixing it in the view avoids perturbing P3's validated chain scoring. | ✅ |

| 25 | 2026-06-29 | **Desire↔Problem linkage in the register** is read from `Desire.key_attributes["relates_to_problem"]` (matched to a Problem id or name); unlinked Desires are listed separately as "wished-for outcomes". | The ontology has **no edge type** touching `Desire` (only `Problem -[BLOCKS]-> Activity`), so there is no graph relationship to traverse; the completeness field `relates_to_problem` on a Desire is the only available link, and inventing a `Desire→Problem` edge would breach the ontology-constraint rule (it'd need a contract bump + create-gate support). Revisit if Desire grows real edges. | 🔁 revisit |

| 26 | 2026-06-29 | **Deploy = one git-connected Cloudflare Pages project (PWA + Pages Functions).** The key-proxy moved from the standalone Worker into **`pwa/functions/`** (Pages Functions); the PWA and its `/llm` `/stt` `/tts` proxy now deploy together as a single Pages project connected to GitHub — every push auto-deploys, same origin (relative paths unchanged). Proxy logic is a **single shared module** (`pwa/functions/_shared.ts`) imported by both the Pages route files and the retained standalone `worker/` (kept for separate-origin/local use). Secrets set in the Pages dashboard (not git); non-secret vars in `pwa/wrangler.toml`. **ElevenLabs confirmed working on the Starter plan** (the earlier `402` is resolved; ADR #20's "gate pending" is cleared) — TTS+STT verified live through the Functions. | The owner wanted the simplest possible maintenance + deploy story: "push to GitHub, it deploys." One Pages project (vs Pages + a separate Worker) means one origin (no CORS/abs-URL juggling), one pipeline, one place for secrets, and free preview deploys per branch — and `git push` is the entire update workflow. Sharing `_shared.ts` keeps the proxy DRY across both deploy modes so they can't drift. Keeping `worker/` avoids breaking the documented local split-dev flow and gives a fallback. | ✅ |

| 27 | 2026-07-01 | **Phase-11 automatic Drive sync = Apps Script Web App (runs as owner) + Google Drive for Desktop, fronted by a Cloudflare Pages Function.** The PWA auto-pushes its Answer Log (`/sync/answer-log`) on session close and auto-pulls its latest Session Brief (`/sync/brief`) on start; the Pages Function forwards to an Apps Script Web App deployed *Execute-as-me / access-anyone*, which reads/writes the owner's Drive in the **exact FolderBus layout**; Drive for Desktop mirrors that tree to the laptop so `FolderBus` + `cli run-round` work **unchanged**. **(a)** Front door is the Pages Function (not browser→Apps-Script direct): the shared secret stays server-side and the Function follows Apps Script's cross-origin 302 redirect that a browser can't. **(b)** **Raw text canonical store stays the Answer Log JSON** (`answer_logs/{session_id}.json`, write-once); Google Docs as a human transcript mirror is deferred (additive later in `doPost`). **(c)** Chose Drive-for-Desktop over a brain-side `DriveBus` (Drive API + service account) — zero brain code, matches the owner's lightweight/no-extra-service preference. **(d)** `profile.json` writes **merge** (never clobber the brain's `ingested_logs` resume key). The manual download/import (P8) is retained as the offline fallback. | The design always planned a "thin sync endpoint" to replace the manual folder (§2B, §13); Apps Script is the free, serverless way to get one **without any end-user Google login** (it runs as the owner), preserving the seamless PWA-link-only constraint. Reusing the FolderBus layout means the whole laptop side is a config change (`BUS_ROOT`), not code — the seam did its job. Server-side secret + write-once logs keep an "anyone" endpoint safe enough for prototype trust. JSON-as-truth preserves re-derivability (ADR #4) and the schema contract; Docs would lose per-entry provenance. | ✅ |

## Open decisions (resolve when the owning phase starts)

- **STT model validation** — run ElevenLabs Scribe on ~20 real messy recordings before committing P7.
- ✅ **RESOLVED (P3) — Completeness thresholds** — config-driven (`persona/org_satisfied_threshold`, default 0.9); see ADR #16. Re-tune against real interview data once a few personas are populated.
- **Embedding model + similarity-ceiling value** for the create gate (P2).
