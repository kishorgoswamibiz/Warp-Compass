# Local secrets for `wrangler dev` (copy to .dev.vars — which is git-ignored — and fill in).
# In production these are set with `wrangler secret put <NAME>` instead, never committed.
# The same keys already live in brain/.env for batch use.
DEEPSEEK_API_KEY=sk-...
ELEVENLABS_API_KEY=...
