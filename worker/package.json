{
  "name": "warp-compass-worker",
  "private": true,
  "version": "0.1.0",
  "description": "Warp Compass — Cloudflare Worker key proxy. Holds API keys; forwards live calls to DeepSeek + ElevenLabs. Keys never reach the browser.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240725.0",
    "typescript": "^5.5.3",
    "wrangler": "^3.65.0"
  }
}
